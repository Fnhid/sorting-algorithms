#pragma once
#include "sort.h"
#include <vector>
#include <future>


#define NUM_SORTS 11

int main();
void genRandomArr(int* arr);
long long min(long long a, long long b);
long long max(long long a, long long b);